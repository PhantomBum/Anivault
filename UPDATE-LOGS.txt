AniVault Unvaulted — changelog (see desktop/src/renderer/data/update-logs.txt for full history)

2.0.3
- **Gallery from Watch** (frame); **thread JSON import/export**; **HLS → .ts** offline attempt; **clip duration** presets. **Tag v2.0.3**.

2.0.2
- **Community:** local-only per-space threads (device storage). **Watch:** save PNG frame + experimental 10s WebM clip. **Tag v2.0.2**.

2.0.1
- **Hub routes shipped:** `/community`, `/gallery`, `/clips`, and `/browse` load real pages (companion API for community/gallery; dense catalog browse). Sidebar **Community hub**, command palette entries, i18n subtitles. **Tag v2.0.1**.

2.0.0
- **Community release:** **62+** catalogued improvements in the bundled log (install/updates, startup reliability including **CSP removal** for `anivault://`, search/sort, watch, settings, performance). **Tag v2.0.0** → **`AniVaultUnvaultedSetup.exe`** on [GitHub Releases](https://github.com/PhantomBum/Anivault/releases/latest). If the window was ever a black screen, install this build first.

1.2.5
- **Fix:** `anivault://` renderer URL in production; search inputs stay editable while results load; richer sort options. Tag **v1.2.5**.

1.2.4
- **Fix:** Black window — turn off `nodeIntegration` in the shell (preload-only), relax `webSecurity` for packaged `file://`, load index via `pathToFileURL`. Tag **v1.2.4**.

1.2.3
- **Fix:** Black/blank window in packaged build — remove `crossorigin` from renderer `index.html` (Electron `file://` + module scripts). Tag **v1.2.3**.

1.2.2
- **Windows installer:** Branded Squirrel build (`electron-squirrel-startup`, setup icon & loading GIF, Forge metadata). Tag **v1.2.2**.

1.2.1
- **Hotfix:** Black screen on launch — missing `getWindowControls` import in config context (ReferenceError). Tag **v1.2.1**.

1.2.0
- Playback & shell: optional **always-on-top** main window (Settings → Playback); IPC `window:set-always-on-top`. Dialogs: **showCloseButton** for update + command palette. Performance: **in-flight dedupe** in `ani-session-cache` for details/episodes; vendor **manualChunks** in renderer Vite build. Tag **v1.2.0**.

1.1.0
- **Unvaulted previews:** Community, Gallery, and Clips use the same gradient “Studio” visual language (`AvFutureSurface`); per-route copy, feature chips, link to Settings → Studio. Studio tab refactored to the shared component. Tag **v1.1.0**.

1.0.8
- Major UX: reorganized nav & coming-soon routes; Settings split with Privacy & Shortcuts tabs; command palette (Ctrl+Shift+P); scroll restore per route; watch deep links & breadcrumbs; lists bulk remove; continue-watching remove from context menu; diagnostics copy; Alt+, cycles Settings tabs; Alt+Left back. See desktop update log for detail. Tag **v1.0.8**.

1.0.7
- Settings stability fix, Studio tab, QoL. Installer **AniVaultUnvaultedSetup.exe** on GitHub Releases (tag **v1.0.7**).

1.0.0
- Stable **AniVault Unvaulted** line: installer AniVaultUnvaultedSetup.exe, app anivault-unvaulted.exe. See desktop README and bundled update log in the app.

1.0.0-alpha.3
- Windows build and GitHub Releases publishing

1.0.0-alpha.2
- Dependency install reliability for automated builds

1.0.0-alpha.1
- Windows installer (AniVaultSetup.exe)
- Watch page mini player (progress, transport)
- Settings: update logs
