/**
 * User-visible product name for the stable desktop line (window title, shell, installers).
 * Internal package / IPC identifiers remain `anivault` for compatibility.
 */
export const APP_DISPLAY_NAME = "AniVault Unvaulted";

/** Short label for tight UI (e.g. poster placeholders). */
export const APP_SHORT_BADGE = "Unvaulted";
